<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.resumemanager.mapper.ChatBotMapper">
    <resultMap id="chatBotResultMap" type="com.example.resumemanager.model.ChatBot">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="wechatId" column="wechat_id"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="status" column="status"/>
        <result property="boundResumeCount" column="bound_resume_count"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findPage" resultMap="chatBotResultMap">
        SELECT id, name, wechat_id, avatar_url, status, bound_resume_count, created_at
        FROM chat_bots
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR wechat_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY created_at DESC, id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM chat_bots
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR wechat_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="findById" resultMap="chatBotResultMap">
        SELECT id, name, wechat_id, avatar_url, status, bound_resume_count, created_at
        FROM chat_bots
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_bots (name, wechat_id, avatar_url, status, bound_resume_count, created_at)
        VALUES (#{name}, #{wechatId}, #{avatarUrl}, #{status}, COALESCE(#{boundResumeCount}, 0), COALESCE(#{createdAt}, CURRENT_TIMESTAMP))
    </insert>

    <update id="updateStatus">
        UPDATE chat_bots
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
